let numeroTabuada = parseInt(prompt("Digite um número para a tabuada:"));
for (let i = 1; i <= 100; i++) {
    console.log(`${numeroTabuada} x ${i} = ${numeroTabuada * i}`);
}

// tabuada fizemos em aula tb